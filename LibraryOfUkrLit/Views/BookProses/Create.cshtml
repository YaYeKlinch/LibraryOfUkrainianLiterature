@model LibraryOfUkrLit.Models.BookProse

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>BookProse</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BooksID" class="control-label">Book</label>
                <select asp-for="BooksID" class ="form-control" asp-items="ViewBag.BooksID"></select>
            </div>
            <div class="form-group">
                <label asp-for="ProseID" class="control-label">Prose</label>
                <select asp-for="ProseID" class ="form-control" asp-items="ViewBag.ProseID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
