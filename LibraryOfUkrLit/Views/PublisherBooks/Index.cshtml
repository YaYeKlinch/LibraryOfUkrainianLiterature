@model IEnumerable<LibraryOfUkrLit.Models.PublisherBook>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Publisher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PublisherBookID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PublisherBookID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PublisherBookID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
