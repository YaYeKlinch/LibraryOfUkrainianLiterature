@model LibraryOfUkrLit.Models.AuthorPoem

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>AuthorPoem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AuthorID" class="control-label">Author</label>
                <select asp-for="AuthorID" class ="form-control" asp-items="ViewBag.AuthorID"></select>
            </div>
            <div class="form-group">
                <label asp-for="PoemID" class="control-label">Poem</label>
                <select asp-for="PoemID" class ="form-control" asp-items="ViewBag.PoemID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
